{
  "name": "trial",
  "version": "1.0.0",
  "description": "Sample Trial Project",
  "main": "trial.js",
  "private": true,
  "dependencies": {
    "bindings": "~1.4.0",
    "install": "^0.13.0",
    "node-addon-api": "^3.2.1",
    "readline": "^1.3.0"
  },
  "scripts": {
    "install": "cmake-js compile && cd JSLinkage && npm install",
    "test": "cd JSLinkage && npm start"
  },
  "gypfile": true
}
